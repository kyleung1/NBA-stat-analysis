describe("Analysis Api", () => {
  test("returns an object with objects of mean, median, and mode", async () => {});
});

describe("Parse Api", () => {
  test("returns an object with objects of mean, median, and mode", async () => {});
});
